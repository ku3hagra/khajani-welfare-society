import GetInvolvedPage from "./GetInvolvedPage";

// Re-export GetInvolvedPage for backwards compatibility with any existing /volunteer imports
const VolunteerPage = () => {
  return <GetInvolvedPage />;
};

export default VolunteerPage;
