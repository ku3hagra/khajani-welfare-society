/**
 * Reveal.tsx — backwards-compatible re-export.
 * All existing pages import { Reveal } from "@/components/Reveal".
 * This now powers them with anime.js via AnimeReveal.
 */
export { AnimeReveal as Reveal } from "./AnimeReveal";
